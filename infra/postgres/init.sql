-- =========================================================
-- 이 파일은 PostgreSQL 초기화용 SQL 파일(init.sql)입니다.
--
-- Docker Compose에서 이 파일을
-- /docker-entrypoint-initdb.d/ 폴더에 넣어두면,
-- ✅ "DB 데이터 볼륨이 처음 생성될 때(=완전 첫 실행)" 딱 1번만 자동 실행됩니다.
--
-- 이미 볼륨(pg_data / postgres_data 등)이 존재하면
-- ❌ 다시 실행되지 않습니다. (데이터 보호 목적)
--
-- 지금은 빈 파일로 두고,
-- 나중에 필요하면 "초기 DB/테이블/계정 생성 SQL"을 여기에 추가합니다.
-- =========================================================


-- 이 파일은 "볼륨(데이터 저장소)"이 처음 만들어질 때만 실행됩니다.
-- (즉, 컨테이너를 재시작해도 볼륨이 남아있으면 실행되지 않음)
-- 나중에 초기 SQL이 필요할 때 여기에 추가해서 관리하기 위해 빈 파일로 유지합니다.
